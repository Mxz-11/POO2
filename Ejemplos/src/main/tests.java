package main;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;


class tests {

	//Para crear los test, creas un paquete nuevo. New -> Other -> JUnit test.
	@Test
	void test() {
		//Para hacer tests se pone assertEquals(valor esperado, valor obtenido);
		assertEquals(true, true);
	}

}
